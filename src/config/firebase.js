// Firebase is not configured — using mock objects so the app runs without credentials.
// Replace these with real Firebase SDK calls once you have API keys.

export const auth = null;
export const db   = null;
export const storage = null;
